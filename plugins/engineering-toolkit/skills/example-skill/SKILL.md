---
description: Starter skill template. Replace this with a real SaaS engineering skill. Delete once you add your own.
---

# Example Skill

This is a placeholder demonstrating the plugin skill layout. A skill is a
directory under `skills/` containing a `SKILL.md` with YAML frontmatter
(`description` is required — Claude uses it to decide when to invoke the skill)
followed by the instructions Claude should follow.

Replace this file with a real skill, or delete this directory once the plugin
has its own skills.
